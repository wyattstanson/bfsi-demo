# ---- Core (installs cleanly on Python 3.11-3.13; the demo runs on these alone) ----
fastapi>=0.110
uvicorn[standard]>=0.29
streamlit>=1.30          # shareable UI (Streamlit Community Cloud deploy target)
pydantic>=2.6
python-dotenv>=1.0
pyyaml>=6.0
numpy>=1.26
scikit-learn>=1.4
faker>=24.0
httpx>=0.27
networkx>=3.2
pytest>=8.0

# ---- Named production libraries (used automatically if importable) ----
# On Python 3.13 some of these may lack wheels; the code falls back cleanly.
xgboost>=2.0
# redis>=5.0            # online store; falls back to in-process dict if absent
# psycopg[binary]>=3.1  # Postgres driver; falls back to stdlib sqlite3 if absent
# pgvector>=0.2         # vector column type for Postgres

# ---- Optional / heavy (guarded imports, pure-Python fallbacks provided) ----
# econml>=0.15          # uplift X-learner; falls back to sklearn T-learner
# langgraph>=0.2        # agent graph; falls back to a hand-rolled state machine
# anthropic>=0.40       # grounded LLM; falls back to deterministic offline stub
# shap>=0.44            # explanations; falls back to a linear SHAP-style attribution
# torch-geometric       # GNN fraud score; falls back to networkx pagerank
